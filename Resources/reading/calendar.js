var currentWin = Ti.UI.currentWindow;
/*Titanium.Calendar = Ti.Calendar = require ('ag.calendar');
Ti.Calendar.dataSource("coredata");

var calendar = Ti.Calendar.createView({
	top:0,
	editable:true,
	color: "white"
});
currentWin.add(calendar);
Titanium.Admob = require('ti.admob');
var adview = Titanium.Admob.createView({
	top:0,
	testing:true,
	adBackgroungColor: 'black',
	primaryTextColor: 'blue',
	secondaryTextColor: 'green',
	publisherId: '1233'
});
currentWin.add(adview);*/
